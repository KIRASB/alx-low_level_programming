#include "main.h"
#include <stdio.h>
/**
 * main - the function prints her name
 * @argc: the counter of the argumments
 * @argv: the
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
