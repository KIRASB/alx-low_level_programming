#include "main.h"
/**
 * rev_string - flip a string
 * @s: the intpt trng
 * Return: reversed the first input
 */

void rev_string(char *s)
{
	char a = s[0];
	int b = 0;
	int c;

	while (s[b] = '\0')
	{
		b++;
	}
	for (c = 0; c < b; c++)
	{
		b--;
		a = s[i];
		s[i] = s[b];
		s[b] = a;
	}
}
