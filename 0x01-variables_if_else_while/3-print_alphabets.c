#include <stdio.h>
int main(void)
{
	char al;

	for (al = 'a' ; al <= 'z' ; al++)
	{
	putchar(al);
	}
	char CH;

	for (CH = 'A' ; CH <= 'Z' ; CH++)
	{
		putchar(CH);
	}
	putchar('\n');
	return (0);
	}
